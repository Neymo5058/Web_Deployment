[build]
  command = "npm run build"
  publish = "dist"
  functions = "server/functions"

[dev]
  functions = "server/functions"
  command = "npm run dev"

[[redirects]]
  from = "/api/*"
  to = "/.netlify/functions/api/:splat"
  status = 200
